// Code generated by entc, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/lxxonx/cinder-server/ent/group"
	"github.com/lxxonx/cinder-server/ent/pic"
	"github.com/lxxonx/cinder-server/ent/user"
)

// PicCreate is the builder for creating a Pic entity.
type PicCreate struct {
	config
	mutation *PicMutation
	hooks    []Hook
}

// SetUserID sets the "user_id" field.
func (pc *PicCreate) SetUserID(s string) *PicCreate {
	pc.mutation.SetUserID(s)
	return pc
}

// SetNillableUserID sets the "user_id" field if the given value is not nil.
func (pc *PicCreate) SetNillableUserID(s *string) *PicCreate {
	if s != nil {
		pc.SetUserID(*s)
	}
	return pc
}

// SetGroupID sets the "group_id" field.
func (pc *PicCreate) SetGroupID(s string) *PicCreate {
	pc.mutation.SetGroupID(s)
	return pc
}

// SetNillableGroupID sets the "group_id" field if the given value is not nil.
func (pc *PicCreate) SetNillableGroupID(s *string) *PicCreate {
	if s != nil {
		pc.SetGroupID(*s)
	}
	return pc
}

// SetAdress sets the "adress" field.
func (pc *PicCreate) SetAdress(s string) *PicCreate {
	pc.mutation.SetAdress(s)
	return pc
}

// SetCreatedAt sets the "createdAt" field.
func (pc *PicCreate) SetCreatedAt(t time.Time) *PicCreate {
	pc.mutation.SetCreatedAt(t)
	return pc
}

// SetNillableCreatedAt sets the "createdAt" field if the given value is not nil.
func (pc *PicCreate) SetNillableCreatedAt(t *time.Time) *PicCreate {
	if t != nil {
		pc.SetCreatedAt(*t)
	}
	return pc
}

// SetUpdatedAt sets the "updatedAt" field.
func (pc *PicCreate) SetUpdatedAt(t time.Time) *PicCreate {
	pc.mutation.SetUpdatedAt(t)
	return pc
}

// SetNillableUpdatedAt sets the "updatedAt" field if the given value is not nil.
func (pc *PicCreate) SetNillableUpdatedAt(t *time.Time) *PicCreate {
	if t != nil {
		pc.SetUpdatedAt(*t)
	}
	return pc
}

// SetReadAt sets the "readAt" field.
func (pc *PicCreate) SetReadAt(t time.Time) *PicCreate {
	pc.mutation.SetReadAt(t)
	return pc
}

// SetNillableReadAt sets the "readAt" field if the given value is not nil.
func (pc *PicCreate) SetNillableReadAt(t *time.Time) *PicCreate {
	if t != nil {
		pc.SetReadAt(*t)
	}
	return pc
}

// SetUser sets the "user" edge to the User entity.
func (pc *PicCreate) SetUser(u *User) *PicCreate {
	return pc.SetUserID(u.ID)
}

// SetGroup sets the "group" edge to the Group entity.
func (pc *PicCreate) SetGroup(g *Group) *PicCreate {
	return pc.SetGroupID(g.ID)
}

// Mutation returns the PicMutation object of the builder.
func (pc *PicCreate) Mutation() *PicMutation {
	return pc.mutation
}

// Save creates the Pic in the database.
func (pc *PicCreate) Save(ctx context.Context) (*Pic, error) {
	var (
		err  error
		node *Pic
	)
	pc.defaults()
	if len(pc.hooks) == 0 {
		if err = pc.check(); err != nil {
			return nil, err
		}
		node, err = pc.sqlSave(ctx)
	} else {
		var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
			mutation, ok := m.(*PicMutation)
			if !ok {
				return nil, fmt.Errorf("unexpected mutation type %T", m)
			}
			if err = pc.check(); err != nil {
				return nil, err
			}
			pc.mutation = mutation
			if node, err = pc.sqlSave(ctx); err != nil {
				return nil, err
			}
			mutation.id = &node.ID
			mutation.done = true
			return node, err
		})
		for i := len(pc.hooks) - 1; i >= 0; i-- {
			if pc.hooks[i] == nil {
				return nil, fmt.Errorf("ent: uninitialized hook (forgotten import ent/runtime?)")
			}
			mut = pc.hooks[i](mut)
		}
		if _, err := mut.Mutate(ctx, pc.mutation); err != nil {
			return nil, err
		}
	}
	return node, err
}

// SaveX calls Save and panics if Save returns an error.
func (pc *PicCreate) SaveX(ctx context.Context) *Pic {
	v, err := pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pc *PicCreate) Exec(ctx context.Context) error {
	_, err := pc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pc *PicCreate) ExecX(ctx context.Context) {
	if err := pc.Exec(ctx); err != nil {
		panic(err)
	}
}

// defaults sets the default values of the builder before save.
func (pc *PicCreate) defaults() {
	if _, ok := pc.mutation.CreatedAt(); !ok {
		v := pic.DefaultCreatedAt()
		pc.mutation.SetCreatedAt(v)
	}
	if _, ok := pc.mutation.UpdatedAt(); !ok {
		v := pic.DefaultUpdatedAt()
		pc.mutation.SetUpdatedAt(v)
	}
	if _, ok := pc.mutation.ReadAt(); !ok {
		v := pic.DefaultReadAt()
		pc.mutation.SetReadAt(v)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pc *PicCreate) check() error {
	if _, ok := pc.mutation.Adress(); !ok {
		return &ValidationError{Name: "adress", err: errors.New(`ent: missing required field "Pic.adress"`)}
	}
	if v, ok := pc.mutation.Adress(); ok {
		if err := pic.AdressValidator(v); err != nil {
			return &ValidationError{Name: "adress", err: fmt.Errorf(`ent: validator failed for field "Pic.adress": %w`, err)}
		}
	}
	if _, ok := pc.mutation.CreatedAt(); !ok {
		return &ValidationError{Name: "createdAt", err: errors.New(`ent: missing required field "Pic.createdAt"`)}
	}
	if _, ok := pc.mutation.UpdatedAt(); !ok {
		return &ValidationError{Name: "updatedAt", err: errors.New(`ent: missing required field "Pic.updatedAt"`)}
	}
	if _, ok := pc.mutation.ReadAt(); !ok {
		return &ValidationError{Name: "readAt", err: errors.New(`ent: missing required field "Pic.readAt"`)}
	}
	return nil
}

func (pc *PicCreate) sqlSave(ctx context.Context) (*Pic, error) {
	_node, _spec := pc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{err.Error(), err}
		}
		return nil, err
	}
	id := _spec.ID.Value.(int64)
	_node.ID = int(id)
	return _node, nil
}

func (pc *PicCreate) createSpec() (*Pic, *sqlgraph.CreateSpec) {
	var (
		_node = &Pic{config: pc.config}
		_spec = &sqlgraph.CreateSpec{
			Table: pic.Table,
			ID: &sqlgraph.FieldSpec{
				Type:   field.TypeInt,
				Column: pic.FieldID,
			},
		}
	)
	if value, ok := pc.mutation.Adress(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeString,
			Value:  value,
			Column: pic.FieldAdress,
		})
		_node.Adress = value
	}
	if value, ok := pc.mutation.CreatedAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: pic.FieldCreatedAt,
		})
		_node.CreatedAt = value
	}
	if value, ok := pc.mutation.UpdatedAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: pic.FieldUpdatedAt,
		})
		_node.UpdatedAt = value
	}
	if value, ok := pc.mutation.ReadAt(); ok {
		_spec.Fields = append(_spec.Fields, &sqlgraph.FieldSpec{
			Type:   field.TypeTime,
			Value:  value,
			Column: pic.FieldReadAt,
		})
		_node.ReadAt = value
	}
	if nodes := pc.mutation.UserIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   pic.UserTable,
			Columns: []string{pic.UserColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeString,
					Column: user.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.UserID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	if nodes := pc.mutation.GroupIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.M2O,
			Inverse: true,
			Table:   pic.GroupTable,
			Columns: []string{pic.GroupColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: &sqlgraph.FieldSpec{
					Type:   field.TypeString,
					Column: group.FieldID,
				},
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.GroupID = nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// PicCreateBulk is the builder for creating many Pic entities in bulk.
type PicCreateBulk struct {
	config
	builders []*PicCreate
}

// Save creates the Pic entities in the database.
func (pcb *PicCreateBulk) Save(ctx context.Context) ([]*Pic, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pcb.builders))
	nodes := make([]*Pic, len(pcb.builders))
	mutators := make([]Mutator, len(pcb.builders))
	for i := range pcb.builders {
		func(i int, root context.Context) {
			builder := pcb.builders[i]
			builder.defaults()
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PicMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				nodes[i], specs[i] = builder.createSpec()
				var err error
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{err.Error(), err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				if specs[i].ID.Value != nil {
					id := specs[i].ID.Value.(int64)
					nodes[i].ID = int(id)
				}
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pcb *PicCreateBulk) SaveX(ctx context.Context) []*Pic {
	v, err := pcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcb *PicCreateBulk) Exec(ctx context.Context) error {
	_, err := pcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcb *PicCreateBulk) ExecX(ctx context.Context) {
	if err := pcb.Exec(ctx); err != nil {
		panic(err)
	}
}
